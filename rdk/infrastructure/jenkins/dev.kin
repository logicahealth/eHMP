"rdk-dev-build-%branch%" {
	template = 'templates/gradle-dev-build.tpl'
	description = "rdk dev build for the branch %branch%"

  gitUrl = 'https://code.vistacore.us/scm/app/rdk.git'
	gitBranch = '%branch%'

  rootDirectory = 'product'
	gradleTasks = 'generateVersionPropertiesFile test installNpmOracleDB upload'
}
