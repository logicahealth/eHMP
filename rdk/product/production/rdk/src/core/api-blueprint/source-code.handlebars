<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{filename}} - API Blueprint Source</title>
        <style>
            body {
                margin: 0;
            }
            pre {
                margin: 0;
                line-height: 0;
                counter-reset: line;
            }
            pre .line {
                display: block;
                line-height: 1.2em;
            }
            pre .line:before {
                background-color: #f6f6ff;
                counter-increment: line;
                content: counter(line);
                display: inline-block;
                border-right: 1px solid #ddd;
                text-align: right;
                padding-right: 0.5em;
                margin-right: 0.5em;
                width: 2.8em;
                color: gray;
            }
            .warning {
                border-bottom: 2px dotted #f60;
            }
        </style>
    </head>
    <body>
        <pre>
{{#each lines}}
<a name="{{@index}}"/><span class="line">{{{this}}}</span>
{{/each}}
        </pre>
    </body>
</html>